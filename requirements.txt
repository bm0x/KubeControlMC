# KubeControlMC - dependencias completas (TUI + GUI)
# NOTA: El núcleo (src/core) es 100% stdlib, sin requests/aiohttp/psutil/pyperclip.
#
# Raspberry Pi (recomendado, ahorra RAM):  pip install -r requirements-pi.txt

textual>=0.40.0,<9
rich>=13.0.0,<16
customtkinter
Pillow